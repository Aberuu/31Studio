import Navigation from './Navigation';

const Header = () => {
  return (
    <header id="header">
        <a href="#" className="logo">31Studio</a>
      <Navigation />
    </header>
  );
};

export default Header;
